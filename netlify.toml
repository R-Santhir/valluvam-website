# Netlify configuration for Valluvam

[build]
  publish = "."

# Redirect /admin to the CMS
[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200

# Enable Netlify Identity for CMS login
[template.environment]
  NETLIFY_IDENTITY = "true"
